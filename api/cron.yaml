cron:
- description: "Daily US State Tweet update"
  url: /tasks/updateUSTweets
  schedule: every 24 hours
  target: backend
- description: "monday morning mailout"
  url: /StartConsumer
  schedule: every 40 minutes
  target: backend
- description: "Start the consumer service"
  url: /StartProducer
  schedule: every 30 minutes
  description: "Start the producer service"
  target: backend
  
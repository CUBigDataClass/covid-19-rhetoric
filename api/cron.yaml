cron:
- description: "Daily US State Tweet scrape"
  url: /tasks/updateUSTweets
  schedule: every 24 hours
  target: backend
- description: "Start the consumer service"
  url: /StartConsumer
  schedule: every 2 hours
  target: backend
- description: "Start the producer service"
  url: /StartProducer 
  schedule: every 2 hours 
  target: backend
  